SELECT sport.libelle, membres.nom, inscription.id
FROM inscription
JOIN sport ON sport.id = inscription.id
JOIN membres ON membres.id = inscription.id

SELECT sport.libelle, membres.nom, inscription.id

FROM inscription, sport, membres
WHERE inscription.sport = sport.id
AND inscription.membre = membres.id;